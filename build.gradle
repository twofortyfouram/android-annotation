buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath group:'com.android.tools.build', name:'gradle', version:'[0.10.0,0.11['
    }
}

allprojects {
    repositories {
        mavenCentral()
    }
}
